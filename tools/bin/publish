#!/bin/bash

# yarn version

yarn test

yarn compile-src

./tools/bin/prepublish

(
  cd ./work/dist
  npm version patch --no-git-tag-version
  npm publish --access public
)

./tools/bin/postpublish

